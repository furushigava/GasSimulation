# Архитектура и реализация

---
[← README](../README.md) | [Физика](physics.md) | [Математика](mathematics.md) | **Реализация** | [Параметры](parameters.md) | [Графики](graphs.md)

---

## Содержание

- [Обзор архитектуры](#обзор-архитектуры)
- [Структура модулей](#структура-модулей)
- [Потоки данных](#потоки-данных)
- [Ключевые классы](#ключевые-классы)
  - [GasParticle](#gasparticle)
  - [SimulationWidget](#simulationwidget)
  - [MainWindow](#mainwindow)
  - [GraphWindow](#graphwindow)
  - [ConfigWindow](#configwindow)
  - [AppConfig](#appconfig)
- [Технологии](#технологии)
- [Расширение функциональности](#расширение-функциональности)

---

## Обзор архитектуры

Проект построен по модульной архитектуре с разделением ответственности:

```
┌─────────────────────────────────────────────────────────────┐
│                        main.py                              │
│                    (точка входа)                            │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                      MainWindow                             │
│              (главное окно приложения)                      │
│  ┌──────────────────┬──────────────────┬─────────────────┐  │
│  │  Панель          │  SimulationWidget │   Панель       │  │
│  │  управления      │  (визуализация)   │   информации   │  │
│  └──────────────────┴──────────────────┴─────────────────┘  │
└───────┬─────────────────────┬───────────────────┬───────────┘
        │                     │                   │
        ▼                     ▼                   ▼
┌───────────────┐   ┌─────────────────┐   ┌───────────────┐
│ ConfigWindow  │   │   GraphWindow   │   │   AppConfig   │
│ (настройки)   │   │   (12 вкладок)  │   │   (Pydantic)  │
└───────────────┘   └─────────────────┘   └───────────────┘
```

---

## Структура модулей

```
GasSimulation/
│
├── main.py                 # Точка входа приложения
├── config.py               # Legacy-конфигурация (для совместимости)
│
├── models/                 # Модели данных
│   ├── __init__.py
│   └── particle.py         # Класс GasParticle
│
├── simulation/             # Физический движок
│   ├── __init__.py
│   └── widget.py           # SimulationWidget
│
├── ui/                     # Пользовательский интерфейс
│   ├── __init__.py
│   ├── main_window.py      # MainWindow
│   └── config_window.py    # ConfigWindow
│
├── graphs/                 # Визуализация и анализ
│   ├── __init__.py
│   ├── graph_window.py     # GraphWindow (контейнер вкладок)
│   ├── thermodynamic.py    # P-V-T диаграммы
│   ├── distribution.py     # Распределение Максвелла
│   ├── boltzmann.py        # Распределение Больцмана
│   ├── energy_conservation.py  # Сохранение энергии
│   ├── entropy.py          # Энтропия и H-функция
│   ├── brownian.py         # Броуновское движение
│   ├── ergodic.py          # Эргодическая гипотеза
│   ├── rotational.py       # Вращательные степени свободы
│   ├── kinetic.py          # Кинетические характеристики
│   ├── correlation.py      # Корреляционный анализ
│   ├── advanced.py         # Расширенная аналитика
│   └── realtime.py         # Графики реального времени
│
└── schemas/                # Pydantic-схемы конфигурации
    ├── __init__.py         # AppConfig (главная конфигурация)
    ├── base.py             # Базовый класс ConfigSection
    ├── simulation.py       # SimulationConfig
    ├── particles.py        # ParticlesConfig, MoleculeConfig
    ├── physics.py          # PhysicsConfig, потенциалы
    ├── ui.py               # UIConfig
    ├── graphs.py           # GraphsConfig
    └── colors.py           # ColorScheme
```

---

## Потоки данных

### Основной цикл симуляции

```
┌─────────────┐    update()     ┌─────────────────────┐
│   QTimer    │ ───────────────►│  SimulationWidget   │
└─────────────┘                 │                     │
      ▲                         │  1. move_particles()│
      │                         │  2. wall_collisions()│
      │                         │  3. particle_collisions()│
      │                         │  4. apply_potentials()│
      │                         │  5. calculate_stats()│
      │                         │                     │
      │                         └──────────┬──────────┘
      │                                    │
      │                                    │ data_updated (signal)
      │                                    ▼
      │                         ┌─────────────────────┐
      │                         │     GraphWindow     │
      │                         │                     │
      │                         │  update_all_graphs()│
      └─────────────────────────┴─────────────────────┘
```

### Поток конфигурации

```
┌─────────────────┐              ┌─────────────────┐
│  config.json    │◄────────────►│    AppConfig    │
│  (persistence)  │   save/load  │    (Pydantic)   │
└─────────────────┘              └────────┬────────┘
                                          │
                    ┌─────────────────────┼─────────────────────┐
                    │                     │                     │
                    ▼                     ▼                     ▼
          ┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
          │  ConfigWindow   │   │ SimulationWidget│   │   GraphWindow   │
          │  (редактирование)│   │ (использование) │   │ (использование) │
          └─────────────────┘   └─────────────────┘   └─────────────────┘
```

---

## Ключевые классы

### GasParticle

**Файл**: `models/particle.py`

Класс, представляющий отдельную частицу газа.

```python
class GasParticle:
    # Позиция
    x: float          # координата X
    y: float          # координата Y
    
    # Скорость
    v: float          # модуль скорости
    a: float          # угол направления движения (радианы)
    
    # Свойства
    mass: float       # масса частицы
    radius: float     # радиус частицы
    
    # Вращение (для многоатомных молекул)
    theta: float      # угол ориентации
    omega: float      # угловая скорость
    I: float          # момент инерции
    
    # Молекулярная структура
    molecule_type: str     # "monatomic", "diatomic", "polyatomic"
    atoms_count: int       # количество атомов
    atom_positions: list   # относительные позиции атомов
```

**Основные методы**:

| Метод | Описание |
|-------|----------|
| `move(dt)` | Перемещение частицы на шаг времени dt |
| `get_velocity()` | Возврат вектора скорости (vx, vy) |
| `set_velocity(vx, vy)` | Установка скорости из компонент |
| `kinetic_energy()` | Расчёт кинетической энергии |
| `rotational_energy()` | Расчёт вращательной энергии |
| `draw(painter)` | Отрисовка частицы на canvas |
| `draw_molecule(painter)` | Отрисовка молекулярной структуры |

### SimulationWidget

**Файл**: `simulation/widget.py`

Ядро физического движка. Наследует `QWidget`.

**Основные атрибуты**:

```python
class SimulationWidget(QWidget):
    particles: list[GasParticle]  # список частиц
    width: int                     # ширина контейнера
    height: int                    # высота контейнера
    dt: float                      # шаг времени
    
    # Статистика
    total_momentum_x: float        # суммарный импульс (X)
    total_momentum_y: float        # суммарный импульс (Y)
    collision_count: int           # счётчик столкновений
    
    # Режимы
    mode: str                      # текущий режим изменения состояния
    isolated_system: bool          # флаг изолированной системы
    gravity_enabled: bool          # флаг гравитации
    brownian_mode: bool            # флаг броуновского режима
```

**Сигналы PyQt**:

| Сигнал | Параметры | Описание |
|--------|-----------|----------|
| `data_updated` | dict | Испускается при обновлении статистики |
| `log_message` | str | Сообщение для журнала |

**Основные методы**:

| Метод | Описание |
|-------|----------|
| `update()` | Главный цикл: движение, столкновения, статистика |
| `init_particles()` | Инициализация частиц |
| `move_particles(dt)` | Перемещение всех частиц |
| `wall_collisions()` | Обработка столкновений со стенками |
| `particle_collisions()` | Обработка межчастичных столкновений |
| `apply_gravity()` | Применение гравитационного ускорения |
| `apply_potentials()` | Расчёт сил из потенциалов взаимодействия |
| `calculate_energy()` | Расчёт полной энергии системы |
| `calculate_entropy()` | Расчёт энтропии |
| `calculate_pressure()` | Расчёт давления |
| `get_statistics()` | Сбор всей статистики для графиков |

**Режимы изменения состояния**:

| Режим | Действие |
|-------|----------|
| `OFF` | Без изменений |
| `heat` | Увеличение скоростей |
| `freeze` | Уменьшение скоростей |
| `expansion` | Увеличение ширины контейнера |
| `compression` | Уменьшение ширины контейнера |
| `heat_expansion` | Нагрев + расширение |
| `heat_compression` | Нагрев + сжатие |
| `cool_expansion` | Охлаждение + расширение |
| `cool_compression` | Охлаждение + сжатие |

### MainWindow

**Файл**: `ui/main_window.py`

Главное окно приложения. Наследует `QMainWindow`.

**Структура интерфейса**:

```
┌─────────────────────────────────────────────────────────────────┐
│  Меню: Файл | Симуляция | Графики | Справка                     │
├───────────────────┬─────────────────────────┬───────────────────┤
│                   │                         │                   │
│  Панель           │                         │  Панель           │
│  управления       │    SimulationWidget     │  информации       │
│                   │    (визуализация)       │                   │
│  [▶ Старт]        │                         │  N: 200           │
│  [⏸ Пауза]        │    ○  ○    ○            │  E: 1234.5        │
│  [⏹ Стоп]         │       ○  ○              │  T: 300.0 K       │
│  [🔄 Сброс]       │    ○     ○   ○          │  P: 1.0 atm       │
│                   │      ○  ○               │  V: 250000        │
│  Режимы:          │                         │                   │
│  ○ Нагрев         │                         │  Режим: OFF       │
│  ○ Охлаждение     │                         │                   │
│  ○ Расширение     │                         │                   │
│  ○ Сжатие         │                         │                   │
│                   │                         │                   │
├───────────────────┴─────────────────────────┴───────────────────┤
│  Журнал симуляции                                               │
│  > Симуляция запущена                                           │
│  > Столкновений: 156                                            │
└─────────────────────────────────────────────────────────────────┘
```

**Основные методы**:

| Метод | Описание |
|-------|----------|
| `start_simulation()` | Запуск симуляции |
| `pause_simulation()` | Приостановка |
| `stop_simulation()` | Полная остановка |
| `reset_simulation()` | Сброс к начальным условиям |
| `open_config()` | Открытие окна настроек |
| `open_graphs()` | Открытие окна графиков |
| `update_info_panel()` | Обновление панели информации |
| `toggle_mode(mode)` | Переключение режима |

### GraphWindow

**Файл**: `graphs/graph_window.py`

Окно с вкладками визуализации. Наследует `QMainWindow`.

**Вкладки** (12 штук):

1. **Термодинамика** (`thermodynamic.py`) — P-T, P-V, V-T диаграммы
2. **Распределения** (`distribution.py`) — распределение Максвелла
3. **Кинетика** (`kinetic.py`) — средний свободный пробег, частота столкновений
4. **Корреляции** (`correlation.py`) — корреляционный анализ
5. **Расширенные** (`advanced.py`) — спектральный анализ, фрактальная размерность
6. **Реального времени** (`realtime.py`) — живые графики
7. **Энергия** (`energy_conservation.py`) — проверка 1-го закона
8. **Броуновское** (`brownian.py`) — траектория, MSD
9. **Больцман** (`boltzmann.py`) — высотное распределение
10. **Энтропия** (`entropy.py`) — S(t), H-функция
11. **Эргодичность** (`ergodic.py`) — сравнение средних
12. **Вращение** (`rotational.py`) — распределение ω

**Основные методы**:

| Метод | Описание |
|-------|----------|
| `update_all_graphs(data)` | Обновление всех графиков |
| `on_data_received(data)` | Слот для сигнала data_updated |
| `export_data()` | Экспорт данных в CSV |
| `save_figures()` | Сохранение графиков как изображений |

### ConfigWindow

**Файл**: `ui/config_window.py`

Окно настроек параметров симуляции. Наследует `QDialog`.

**Особенности**:
- Автоматическая генерация UI из Pydantic-схем
- Валидация ввода в реальном времени
- Группировка параметров по категориям
- Пресеты молекул (аргон, азот, вода и др.)

### AppConfig

**Файл**: `schemas/__init__.py`

Главный класс конфигурации на основе Pydantic.

```python
class AppConfig(BaseModel):
    simulation: SimulationConfig    # параметры симуляции
    particles: ParticlesConfig      # параметры частиц
    molecule: MoleculeConfig        # молекулярная структура
    physics: PhysicsConfig          # физические параметры
    ui: UIConfig                    # настройки интерфейса
    graphs: GraphsConfig            # настройки графиков
    colors: ColorScheme             # цветовая схема
```

**Методы**:

| Метод | Описание |
|-------|----------|
| `load(path)` | Загрузка из JSON-файла |
| `save(path)` | Сохранение в JSON-файл |
| `validate()` | Валидация всех параметров |
| `to_dict()` | Преобразование в словарь |

---

## Технологии

| Технология | Назначение |
|------------|------------|
| **Python 3.8+** | Основной язык |
| **PyQt5** | GUI-фреймворк |
| **NumPy** | Численные вычисления, векторные операции |
| **Matplotlib** | Построение графиков (встроено в PyQt через FigureCanvas) |
| **SciPy** | Статистические тесты (χ², KS), оптимизация, интерполяция |
| **Pydantic** | Валидация конфигурации, сериализация |

---

## Расширение функциональности

### Добавление нового типа графика

1. Создать файл в `graphs/`, например `my_graph.py`
2. Реализовать класс с методом `update(data)`
3. Добавить вкладку в `GraphWindow.setup_tabs()`

```python
# graphs/my_graph.py
from matplotlib.backends.backend_qt5agg import FigureCanvasQTAgg
from matplotlib.figure import Figure

class MyGraphWidget(FigureCanvasQTAgg):
    def __init__(self, parent=None):
        fig = Figure(figsize=(8, 6))
        self.ax = fig.add_subplot(111)
        super().__init__(fig)
    
    def update(self, data: dict):
        self.ax.clear()
        # ... построение графика ...
        self.draw()
```

### Добавление нового потенциала взаимодействия

1. Добавить параметры в `schemas/physics.py`
2. Реализовать расчёт силы в `SimulationWidget.apply_potentials()`

```python
# schemas/physics.py
class MyPotentialConfig(ConfigSection):
    param1: float = Field(default=1.0, ge=0)
    param2: float = Field(default=2.0, ge=0)

# simulation/widget.py
def apply_my_potential(self, r):
    cfg = self.config.physics.my_potential
    # U(r) = ...
    # F(r) = -dU/dr
    return force
```

### Добавление нового параметра конфигурации

1. Добавить поле в соответствующую схему
2. UI сгенерируется автоматически через ConfigWindow

```python
# schemas/simulation.py
class SimulationConfig(ConfigSection):
    my_param: float = Field(
        default=1.0,
        ge=0.0,
        le=10.0,
        description="Описание параметра"
    )
```

---

[← README](../README.md) | [Физика](physics.md) | [Математика](mathematics.md) | **Реализация** | [Параметры](parameters.md) | [Графики](graphs.md)
